{
  "name": "vue3-super-tree-integration-tests",
  "version": "1.0.0",
  "description": "Integration tests for Vue3 Super Tree plugin",
  "type": "module",
  "scripts": {
    "test:integration": "node scripts/test-npm-import.js",
    "test:build": "node scripts/validate-build.js",
    "test:vite": "cd vite-project && npm install && npm run build",
    "test:all": "npm run test:integration && npm run test:build && npm run test:vite",
    "setup": "npm install && cd vite-project && npm install"
  },
  "devDependencies": {
    "typescript": "^5.0.0"
  },
  "engines": {
    "node": ">=16.0.0"
  }
}